@model ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<div class="glass-card">
    <h1 class="page-title text-danger mb-2" style="font-size: 1.5rem;">Something went wrong</h1>
    <p class="text-muted mb-4">An error occurred while processing your request.</p>
    @if (Model.ShowRequestId)
    {
        <p class="mb-0"><strong>Request ID:</strong> <code>@Model.RequestId</code></p>
    }
</div>
